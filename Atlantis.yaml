version: 3

workflow: custom-workflow
workflows:
  custom-workflow:
    plan:
      steps:
        - run: terraform fmt -check
        - run: terraform plan
    apply:
      steps:
        - run: terraform apply -auto-approve
