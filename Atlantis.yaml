version: 3

# Configuring the workflow
workflow:
  plan:
    steps:
      - run: terraform fmt
      - run: terraform plan
  apply:
    steps:
      - run: terraform apply

# Defining the project with a specific directory and name
projects:
  - name: "atlantis-terraform-project"
    dir: "C:/Users/SharkOps011/Desktop/Atlantis-1/Atlantis"
    terraform_version: "latest"
    workflow: "default"
