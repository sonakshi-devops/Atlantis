version: 3
projects:
  - name: "your-project-name"  # Change this to your actual project name
    dir: "C:/Users/SharkOps011/Desktop/Atlantis-1/Atlantis"  # Path to your Terraform files
    terraform_version: "1.1.0"  # Specify the version you want to use
    workflow: "default"
workflows:
  default:
    plan:
      steps:
        - init
        - plan
    apply:
      steps:
        - apply
