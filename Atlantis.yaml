version: 3

projects:
  - name: atlantis-project
    dir: .
    workflow: custom-workflow

workflows:
  custom-workflow:
    plan:
      steps:
        - run: echo "Changed files in last commit:"
        - run: git show --name-only HEAD
        - run: terraform fmt
        - init
        - plan
    apply:
      steps:
        - apply
