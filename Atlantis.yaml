# atlantis.yaml

version: 3

# Adding 'allowed_overrides' section to enable 'workflow' key in the repo config
allowed_overrides: [workflow]

# Optional: Define workflow
workflow:
  plan:
    steps:
      - run: terraform fmt
      - run: terraform plan
  apply:
    steps:
      - run: terraform apply
