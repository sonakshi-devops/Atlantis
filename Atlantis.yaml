version: 3
projects:
  - name: "Atlantis-1"
    dir: "C:/Users/SharkOps011/Desktop/Atlantis-1/Atlantis"   # Directory where your Terraform configuration is located
    terraform_version: "latest"
    workflow: "default"
workflows:
  default:
    plan:
      steps:
        - init
        - plan
    apply:
      steps:
        - apply
